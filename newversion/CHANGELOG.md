# Changelog

## 2.0.0
- Initial ESM-first `@sdo/core` scaffold.
- Added headless core API (`createSEDO`, plugin lifecycle, backup APIs).
- Added navigation export/import, encrypted backups (PBKDF2 + AES-GCM), delta scaffolding.
- Added minimal web UI shell with “+ Додати модуль” button for runtime ESM module loading.
